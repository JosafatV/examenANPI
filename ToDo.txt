EXAMEN ANPI

P1
	P1.1	LISTO
En la fórmula dada la fuerza se encuentra de forma explícita en la expresión $m * d2x/dt$, por lo que solo hace falta despejar este término. al hace esto se obtiene $-b*v-k*x$ y gracias a que ya conocemos el valor de la velocidad, la dependencia con el tiempo se ve en el valor de v con el que se llama a la función.

	P1.2 LISTO
Al conocer los valores de velocidad y posición, en la fórmula de Euler queda solo darle valores a las variables. Para calcular la aceleración se parte de la ecuación $F=ma$ donde el despeje es trivial y lo podemos ver en 1.2

	P1.3 LISTO
Las ecuaciones g(x,v,t) y h(x,v,t) corresponden a la velocidad y aceleración, como es posible despejar con 1.2 $g(x,v,t) = v(t) y h(x,v,t) = 1/m * F(x(t), v(t), t)

	P1.4 Revisar; intentar expresar la velocidad en k2 y k3 como v (i+1/2)
A pesar de la gran cantidad de términos de los que depende g(), al ser esta la velocidad se usan los valores conocidos de esta. Para l() se vuelve a reemplazar con la expresión obtenida de (1.2)

	P1.5 LISTO
Las soluciones tienen forma senoidal cuya amplitud decrece de manera exponencial, con convergencia a 0 cuando el tiempo tiende a infinito, con excepción de la que utilzia el método de Euler con paso de 0.5. Todo parece indicar que el tamaño de paso hace imposible ver el efecto de la amortiguación en el rango de la simulación. Se sabe este comportamiento ya que el sistema está subamortiguado; ya que $0 < b2/4mk = 0.00625 < 1$ esto dice que "el resorte es mas importante que el amortiguador, la solución muere exponencialmente, pero oscila" \cite{benguri}

P2
	2.1 En la primera y última segunda derivada ya no podemos dejar una ecuación simple, resultante de asumir los valores en los extremos. Debemos usar la expresión general que usamos para todos los puntos internos, donde el índice i-1 en cero apunta, en realidad, al índice n y el índice n+1 apunta al cero para generar, algo así como una solución cilíndrica (basado en la solución vista en clase) en donde los punto forman un ciclo en vez de una sección finita. 

Con esto llenamos la matriz siguiendo el caso n visto en clase, donde el sistema de ecuaciones requeiere de dos valores anteriores, con los valores anterios a cero: n y n-1. Llenamos entonces la matriz siguiendo este algoritmo que nos genera una forma tridiagonal con algunos valores en zonas externas, producto de llegar al final de las columnas y tener n+1 apuntando a cero

	2.2
	Fig 2.1 Planteo manual del sistema de ecuaciones

	2.3 No, esto dado que la simplificación que se utilizaba antes nos permitía eliminar dos columnas para generar una matriz tridiagonal basado en que de todas formas no estabamos inventando estos valores. Al no asumir esos valores no podemos eliminar las columnas.

	2.4 Listo, no confirmada

	2.5 Aproximada (mejor)

	2.6 Listo, no confirmada

P3
3.1 Listo, no confirmada

3.2 Listo, no confirmada

3.3 LISTO

3.4 Listo, no confirmada

3.5 Listo, no confirmada

3.6 Listo, no confirmada

3.7 Revisar; no selecciona opción esperada

3.8 Listo, no confirmada

http://www.fis.puc.cl/~rbenguri/SystemMassSpringShock.pdf
